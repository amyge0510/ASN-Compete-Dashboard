requests>=2.32
beautifulsoup4>=4.12
PyYAML>=6.0
anthropic>=0.92
openai>=1.60          # for LLM_PROVIDER=openai (OpenAI / Azure OpenAI credits)

# Optional — only needed for JS-rendered catalogs (sources with `render: true`):
#   pip install playwright && playwright install chromium